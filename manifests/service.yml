apiVersion: v1
kind: Service
metadata:
    name: teomarcdhiotestingazurepipeline
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: teomarcdhiotestingazurepipeline